import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

// Component that maintains a specific aspect ratio for its content
// Useful for ensuring consistent image and video dimensions
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
